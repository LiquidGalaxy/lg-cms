<krpano version="1.16.3">

	<image type="{{ object.projection }}"
		hfov="{{ object.hfov }}"
		vfov="{{ object.vfov }}"
		voffset="{{ object.voffset }}"
	>
		<{{ object.projection|lower }} url="{{ object.storage.url }}" />
	</image>

	<view limitview="on" />
</krpano>
